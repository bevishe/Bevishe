## ECMAScript

客户端脚本语言的标准

* 1.基本语法
  * 1.与html的结合方式 
    * 1.内部js：定义script标签，标签内容就是js代码
    * 2.外部js：定义script标签，通过src属性引入外部的js文件
  * 2.注释
    * 1.单行注释  //注释内容
    * 2.多行注释  /* 注释内容*/
  * 3.数据类型
    * 1.原始数据类型（基本数据类型）:
        * 1.number:数字，整数、小数、NaN（not a number）
        * 2.string:字符、字符串  "abc"/"a"/‘a’
        * 3.boolean：true或者false
        * 4.null：一个对象为空的占位符
        * 5.undefined：未定义。如果一个变量没有给初始化值，则会默认赋值给undefine
    * 2.引用数据类型（对象）
  * 4.变量
    * 变量：一小块存储数据的内存的区域。
    * java语言是强类型的语言，JavaScript是弱类型的语言
    * var 变量名 = 初始化的值；
  * 5.运算符  
    * 1.比较运算符：
        * 1.类型相同 ：直接比就行
            * 字符串是按照字典顺序比较
        * 2.类型不同： 先进行类型转换，再比较
  * 6.流程控制语句   
* 2.基本的对象
* 3.

## function 
* 1.函数对象（方法对象）
    * 1.创建  
        * 1.var fun = new Function(形参，方法体);